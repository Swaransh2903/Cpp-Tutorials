// Disjoint set will tell at any instant that whether 2 nodes belong to the same component or not in constant time O(1).

// For Disjoint Set , we have mainly two functions : findParent() and union(){either by rank or size}

// To understand this question refer to striver video because it is way too complex to explain via comments